# SPDX-FileCopyrightText: Magenta ApS <https://magenta.dk>
# SPDX-License-Identifier: MPL-2.0
[tool.poetry]
name = "mo_sor"
version = "0.0.0"  # versioning handled by conventional commits
description = "OS2mo integration for Sundhedsvæsenets Organisationsregister (SOR)"
authors = ["Magenta ApS <info@magenta.dk>"]
license = "MPL-2.0"

[tool.poetry.dependencies]
python = "^3.11"
pydantic = "^1"
structlog = "^24.1.0"
uvicorn = "^0.34"
fastramqpi = "^12"
fastapi = "^0.115.0"

[tool.poetry.group.pre-commit.dependencies]
mypy = "^1.8.0"
pre-commit = "^3.6.0"
ruff = "^0.4.4"
deptry = "^0.23.0"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.5"
pytest-asyncio = "^0.19.0"
pytest-cov = "^4.1.0"
pytest-split = "^0.8.2"
respx = "^0.21.1"
asgi-lifespan = "^2.1.0"

[tool.poetry.group.dev.dependencies]
ariadne-codegen = "0.7.1"
websockets = "^13.0.1"  # for ariadne

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.mypy]
plugins = "pydantic.mypy"

[tool.ariadne-codegen]
target_package_name = "autogenerated_graphql_client"
target_package_path = "mo_sor/"
client_name = "GraphQLClient"
schema_path = "schema.graphql"  # curl -O http://localhost:5000/graphql/v29/schema.graphql
queries_path = "queries.graphql"
include_all_inputs = false
include_all_enums = false
plugins = [
    "ariadne_codegen.contrib.shorter_results.ShorterResultsPlugin",
    "fastramqpi.ariadne.UnsetInputTypesPlugin",
]
include_comments = false
[tool.ariadne-codegen.scalars.DateTime]
type = "datetime.datetime"
parse = "fastramqpi.ariadne.parse_graphql_datetime"
[tool.ariadne-codegen.scalars.UUID]
type = "uuid.UUID"

[tool.coverage.report]
omit = [
  "mo_sor/autogenerated_graphql_client/*",
]

[tool.deptry]
extend_exclude = [
  "mo_sor/autogenerated_graphql_client/*",
]
[tool.deptry.per_rule_ignores]
DEP002 = [
  # uvicorn is used by the Dockerfile
  "uvicorn",
]
